[package]
name = "genealogy-plugin-host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
genealogy-core = { workspace = true }
genealogy-app = { workspace = true }
wasmtime = { workspace = true }
wasmtime-wasi = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
uuid = { workspace = true }
# The round-trip test reads the events table directly to assert Software-agent provenance was
# recorded (ADR 0011 §5) — no use-case exposes the operator kind.
sqlx = { workspace = true }
# The ui-panel test parses the plugin's opaque JSON payload to confirm it is a well-formed form
# (the host itself never parses it — that is the renderer's job, ADR 0012).
serde_json = { workspace = true }

[lints]
workspace = true
